# Journal — 2026-05-25 — Release v0.2.1

**Phase** : F2 (polish + release)
**Durée estimée** : 10 min
**Statut** : ✅ Complété

---

## Contexte

Tag v0.2.1 — bugfixes F2 accumulés depuis v0.2.0 + mise à jour du modèle recommandé.

---

## Changements depuis v0.2.0

### Corrections LLM
- **Fix erreur LLM silencieuse** — les erreurs LLM sont maintenant affichées via toast (visible pour l'utilisateur)
- **Fix spinner bloqué** — le spinner s'arrête correctement à la réception de l'événement `completed`
- **Fix qwen3 thinking mode** — ajout de `/no_think\n` dans le prompt pour désactiver `<think>` (via `provider.rs`)
- **Fix retry ResponseFormat** — la boucle de retry dans `pipeline.rs` couvre maintenant les erreurs `ResponseFormat` en plus des erreurs de placeholder
- **Filtre segments vides et placeholder-only** — ces segments sont ignorés avant envoi au LLM

### UI / UX
- **QA panel détails d'erreur** — le panneau QA affiche maintenant les détails de chaque erreur
- **Bouton Export** — nouveau bouton Export dans la barre d'outils
- **Select dynamique modèles Ollama** — la liste des modèles disponibles est chargée depuis l'API Ollama au lieu d'être codée en dur
- **Timer traduction par fichier** — durée de traduction affichée par fichier
- **QA line width pixel-based** — détection full-width vs half-width basée sur les pixels (caractères CJK comptent double)

### Internationalisation
- **i18n FR/EN** — interface complète en français et en anglais
- **README bilingue** — README.md (EN) + README.fr.md (FR)

### Modèle recommandé
- **qwen3:4b → qwen3:4b-instruct-2507-q4_K_M** — la variante `-instruct` répond sans phase de raisonnement (plus rapide et plus fiable)
  - `DEFAULT_OLLAMA_MODEL` mis à jour dans `provider.rs`
  - README.md, README.fr.md, `.github/workflows/release.yml` mis à jour

### Version
- `tauri.conf.json` : `0.2.0` → `0.2.1`
- `Cargo.toml` : `0.1.0` → `0.2.1` (synchronisé avec tauri.conf.json)

---

## Fichiers modifiés

- `src-tauri/src/llm/provider.rs` — `DEFAULT_OLLAMA_MODEL` mis à jour
- `README.md` — modèle recommandé + note explicative
- `README.fr.md` — modèle recommandé + note explicative (FR)
- `.github/workflows/release.yml` — modèle recommandé dans releaseBody
- `src-tauri/tauri.conf.json` — version `0.2.1`
- `src-tauri/Cargo.toml` — version `0.2.1`

## Fichiers créés

- `docs/journal/2026-05-25-release-v0.2.1.md` — ce fichier

---

## Tests

- **93 tests** — inchangé (modifications non fonctionnelles)
- `cargo clippy -- -D warnings` : ✅ 0 warnings
- `cargo test` : ✅ 93/93

## Tâches ROADMAP cochées

- aucune (release + housekeeping)

## Prochaine session

**F3 — Polissage + VX Ace + beta privée** :
1. Intégration `marshal-rs` pour les fichiers `.rvdata2` (VX Ace)
2. TM v2 — fuzzy matching (Levenshtein, seuil 80 %)
3. Glossaire v1 — CRUD + injection prompt
4. Recrutement 20–30 beta testeurs (Discord / F95zone)

---
*Généré par Claude Code — Hoshi2Star*
