# Journal — 2026-05-24 — F0 Setup Session 2 (dépendances + tooling)

**Phase** : F0
**Durée estimée** : 2h
**Statut** : ✅ Complété (F0 fondations terminées, VSCode à faire)

---

## Ce qui a été fait

- Résolution conflict pnpm v10→v11 (node_modules recréés, `pnpm-workspace.yaml` avec `allowBuilds`)
- Installation dépendances frontend : zustand, @tanstack/react-table, @tanstack/react-virtual, @tanstack/react-query
- Installation Tailwind CSS v4 via `@tailwindcss/vite` plugin + `src/index.css` avec `@import "tailwindcss"`
- Ajout alias path `@/*` dans `tsconfig.json` + plugin `resolve.alias` dans `vite.config.ts`
- Initialisation shadcn/ui (preset Nova, CSS variables, style radix-nova, zinc base color)
- Création structure dossiers `src/` : `components/editor/`, `features/`, `stores/`, `lib/`, `hooks/`
- Ajout dépendances Rust dans `src-tauri/Cargo.toml` : `tokio` (full), `sqlx` (runtime-tokio + sqlite + macros), `thiserror`
- `src-tauri/capabilities/default.json` vérifié : contient `core:default` ✅
- Création `src-tauri/migrations/` avec `.gitkeep`
- Création `src-tauri/src/state.rs` (AppState vide, sera complété en F1)
- Création `src-tauri/src/commands/mod.rs` (vide, commands ajoutées en F1)
- Mise à jour `src-tauri/src/lib.rs` : déclaration modules `state` et `commands`
- Installation `prettier` + `eslint` + plugins TypeScript
- Création `.claude/settings.json` avec 3 hooks (pipe-testés + validés avec jq) :
  - PostToolUse Edit|Write `.rs` → `cargo fmt`
  - PostToolUse Edit|Write `.ts|.tsx` → `prettier --write`
  - PreToolUse Bash → blocage `npm install`, `yarn add`, `git push --force`
- Ajout scripts `typecheck` et `lint` dans `package.json`
- ROADMAP.md mis à jour (F0 items cochés)
- Vérification build : `cargo check` ✅, `cargo clippy -D warnings` ✅, `pnpm typecheck` ✅, `pnpm build` ✅ (915ms)
- `pnpm tauri dev` : Vite démarre en 217ms, Rust compile sans erreur ✅

## Fichiers créés

- `src/index.css` — import Tailwind v4 + variables shadcn Nova
- `src/components/ui/button.tsx` — premier composant shadcn (généré par init)
- `src/lib/utils.ts` — utilitaire `cn()` shadcn
- `src/components/editor/.gitkeep`
- `src/features/.gitkeep`
- `src/stores/.gitkeep`
- `src/hooks/.gitkeep`
- `components.json` — config shadcn (style: radix-nova, base: zinc, CSS vars)
- `src-tauri/migrations/.gitkeep`
- `src-tauri/src/state.rs`
- `src-tauri/src/commands/mod.rs`
- `.claude/settings.json` — hooks PostToolUse + PreToolUse

## Fichiers modifiés

- `package.json` — ajout deps zustand/tanstack, shadcn deps, prettier/eslint, scripts typecheck/lint
- `pnpm-workspace.yaml` — `allowBuilds: esbuild: true, msw: false`
- `vite.config.ts` — ajout `tailwindcss()` plugin + `resolve.alias` pour `@/`
- `tsconfig.json` — ajout `baseUrl` + `paths` alias `@/*`
- `src/main.tsx` — ajout `import "./index.css"`
- `src-tauri/Cargo.toml` — ajout tokio, sqlx, thiserror
- `src-tauri/src/lib.rs` — déclaration modules + commentaire ADR-005
- `ROADMAP.md` — items F0 cochés

## Dépendances ajoutées

**Frontend (pnpm):**
- `zustand` ^5.0.13
- `@tanstack/react-table` ^8.21.3
- `@tanstack/react-virtual` ^3.13.25
- `@tanstack/react-query` ^5.100.13
- `tailwindcss` ^4.3.0 + `@tailwindcss/vite` ^4.3.0
- shadcn init: `clsx`, `tailwind-merge`, `class-variance-authority`, `tw-animate-css`, `radix-ui`, `lucide-react`, `shadcn`, `@fontsource-variable/geist`
- devDeps: `prettier`, `eslint`, `@typescript-eslint/*`, `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh`, `@types/node`

**Rust (Cargo.toml):**
- `tokio = { version = "1", features = ["full"] }`
- `sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite", "macros"] }`
- `thiserror = "2"`

## Problèmes rencontrés

- **pnpm v10→v11 store conflict** : résolu avec `rm -rf node_modules && pnpm install`
- **esbuild build scripts bloqués** : résolu via `pnpm-workspace.yaml allowBuilds: esbuild: true`
- **msw build scripts bloqués** (shadcn init) : résolu via `msw: false` dans `allowBuilds`
- **shadcn init interactif** : contourné avec `-t vite -b radix -p nova -y` + réponse stdin pour "reinstall?"
- **Tailwind non trouvé par shadcn** : Tailwind v4 nécessite `@import "tailwindcss"` dans un fichier CSS ET le plugin vite — ajout de `src/index.css` puis `import "./index.css"` dans `main.tsx`

## Décisions prises

- shadcn preset **Nova** retenu (Radix + Geist font + modern aesthetics) — base color zinc documentée dans components.json
- `prettier` + `eslint` installés en devDeps pour les hooks de formatage
- `src/index.css` crée comme fichier CSS principal (le template Tauri n'en avait pas)
- `AppState` et `commands/mod.rs` créés vides — seront remplis en F1 avec la DB

## Tâches ROADMAP cochées cette session

- [x] Vérification `lib.rs` comme entrée (pas `main.rs`)
- [x] `pnpm dlx shadcn@latest init` (CSS variables: yes, base color: zinc)
- [x] Tailwind v4 configuré
- [x] `pnpm add zustand @tanstack/react-table @tanstack/react-virtual @tanstack/react-query`
- [x] Rust deps ajoutés : `tokio`, `sqlx`, `thiserror`
- [x] `src-tauri/capabilities/default.json` — vérifié avec `core:default`
- [x] `.claude/settings.json` avec hooks (3 hooks pipe-testés)
- [x] `src-tauri/migrations/` créé
- [x] `src-tauri/src/state.rs` créé
- [x] Structure `src/` créée

## Prochaine session (F1)

1. `src-tauri/migrations/0001_initial.sql` — tables projects, source_files, segments
2. `src-tauri/src/db/pool.rs` — init SqlitePool
3. `src-tauri/src/state.rs` — AppState { db: SqlitePool }
4. Commandes Tauri : `open_project`, `get_source_files`, `get_segments`, `update_segment`
5. Layout 3 colonnes React + SegmentGrid (TanStack Table + Virtual)
6. Zustand stores : `editor.ts`, `project.ts`
7. VSCode config (rust-analyzer, .vscode/settings.json, launch.json) — reste de F0

---
*Généré par Claude Code — Hoshi2Star*
